import Palette from "../components/Palette";

const page = () => {
  return (
    <>
      <section className="w-full h-screen flex">
        <Palette />
      </section>
    </>
  );
};

export default page;
